# 5.6 在机器人上编译

> 你已经把代码推到了机器人上。现在，把 C++ 代码变成机器人能运行的**可执行文件**。

---

## 类比：做菜之前先买原材料

```
代码 = 食谱（文字，人能看懂）
编译 = 按食谱准备食材（机器能看懂的过程）
可执行文件 = 成品菜（直接吃/运行）
```

C++ 和 Python 不一样。Python 代码写完了直接 `python3 xxx.py` 就能跑。C++ 需要一个叫 **"编译器"** 的程序把源码翻译成二进制文件。

这个过程叫 **编译（build）**。在 Linux 上用 `cmake` + `make` 两个工具完成：

- `cmake` = "根据食谱列出需要的所有食材，生成采购清单"
- `make` = "按采购清单去买菜、切菜、炒菜"
- 产出的 `g1_ctrl` = "做好的菜"（可执行文件）

---

## 前提：你已经在机器人上了

按照 [5.2](5.2_connect_to_robot.md) 连上机器人：

```bash
ssh unitree@<机器人IP>
```

你已经把 `deploy/` 文件夹推到了机器人的 `~/g1-rl-deploy/` 下（用 `scripts/deploy.sh push`）。

```bash
# 确认文件都在
ls ~/g1-rl-deploy/
# 应该看到：include/  g1_29dof/  install_onnxruntime.sh  README.md
```

---

## 第一步：安装 ONNX Runtime（只做一次）

ONNX Runtime 是一个专门跑 ONNX 模型的库。机器人上没有这个库——需要下载。

```bash
cd ~/g1-rl-deploy
bash install_onnxruntime.sh
```

脚本会：
1. 检查是否已经装过（装过就跳过）
2. 如果没装过 → 从 GitHub 下载（约 40MB）
3. 解压到 `thirdparty/onnxruntime-linux-x64-1.22.0/`

如果机器人不能联网，看 [5.8 排错速查](5.8_troubleshooting.md) 里的 "编译阶段" → "无法联网下载" 章节。

安装成功后，脚本会告诉你设置一个环境变量：

```bash
export ONNXRUNTIME_ROOT=~/g1-rl-deploy/thirdparty/onnxruntime-linux-x64-1.22.0
```

**把这行复制下来**，马上要用。如果你想让下次 SSH 登录也能自动记住，可以把它加到 `~/.bashrc` 末尾：

```bash
echo 'export ONNXRUNTIME_ROOT=~/g1-rl-deploy/thirdparty/onnxruntime-linux-x64-1.22.0' >> ~/.bashrc
```

---

## 第二步：检查编译工具是否齐全

```bash
# 逐个检查
cmake --version        # 应该有版本号输出
g++ --version          # 应该有版本号输出
make --version         # 应该有版本号输出
```

如果哪个命令报 `command not found`，就装它：

```bash
sudo apt install cmake build-essential -y
```

> `build-essential` 里包含了 `g++` 和 `make`，不用分开装。

---

## 第三步：编译

```bash
# 先设置 ONNX Runtime 路径（如果还没设的话）
export ONNXRUNTIME_ROOT=~/g1-rl-deploy/thirdparty/onnxruntime-linux-x64-1.22.0

# 进入源码目录，创建 build 文件夹
cd ~/g1-rl-deploy/g1_29dof
mkdir -p build
cd build

# 生成构建文件（cmake 会检查所有依赖是否齐全）
cmake .. -DCMAKE_BUILD_TYPE=Release

# 开始编译（-j 表示并行编译，用上所有 CPU 核心）
make -j$(nproc)
```

### 正常的话你会看到什么

```
[ 25%] Building CXX object CMakeFiles/g1_29dof_controller_lib.dir/src/State_RLBase.cpp.o
[ 50%] Building CXX object CMakeFiles/g1_29dof_controller_lib.dir/src/State_Mimic.cpp.o
[ 75%] Linking CXX static library libg1_29dof_controller_lib.a
[100%] Building CXX object CMakeFiles/g1_ctrl.dir/main.cpp.o
[100%] Linking CXX executable g1_ctrl
[100%] Built target g1_ctrl
```

最后一行 `Built target g1_ctrl` 出现，说明编译成功。检查一下：

```bash
ls -lh ./g1_ctrl
# -rwxr-xr-x ... g1_ctrl  ← 这就是你刚编译出来的可执行文件
```

---

## 报错了怎么办？

以下是常见编译错误的简单处理：

| 报错信息 | 什么意思 | 怎么办 |
|---------|---------|-------|
| `cmake: command not found` | 没装 cmake | `sudo apt install cmake -y` |
| `fatal error: unitree/robot/...` | 机器人的 SDK 没装 | 联系管理员装 unitree_sdk2 |
| `cannot find -lonnxruntime` | ONNX Runtime 路径不对 | 确认 `export ONNXRUNTIME_ROOT=...` 执行了 |
| `No such file: .../libonnxruntime.so` | ONNX Runtime 没下载成功 | 重新运行 `bash install_onnxruntime.sh` |

其他看不懂的错误，去 [5.8 排错速查](5.8_troubleshooting.md) 找。

---

## 总结

你现在有了一个文件：`~/g1-rl-deploy/g1_29dof/build/g1_ctrl`。

它就是你训练出来的 AI 模型的 "身体"——它用 ONNX Runtime 跑你的神经网络，通过 DDS 和机器人电机通信，让 G1 走路。

---

## 下一步

编译完了，准备运行：[5.7 在机器人上运行](5.7_run_on_robot.md)。
